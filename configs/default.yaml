synthetic:
  n_subjects: 10
  epochs_per_class: 12
  sampling_rate_hz: 128.0
  epoch_duration_s: 4.0
  artifact_probability: 0.05
  random_seed: 2026

preprocessing:
  low_cut_hz: 1.0
  high_cut_hz: 40.0
  peak_to_peak_threshold_uv: 200.0

evaluation:
  n_splits: 5
  random_seed: 2026
